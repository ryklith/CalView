services:
  app:
    build:
      context: .
      args:
        # Optional: override the calendar URL at build time.
        # If unset, the default in src/lib/config.js is used.
        - REACT_APP_CALENDAR_URL=${REACT_APP_CALENDAR_URL:-}
    ports:
      - "80:80"
    restart: unless-stopped
